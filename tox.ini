[tox]
#envlist = py311
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
commands =    
    pytest --cov --no-cov-on-fail
    coverage xml --skip-empty
    #coverage run -m pytest
    #coverage xml

[coverage:run]
relative_files = True
#source = servipagos/src/
#branch = True
